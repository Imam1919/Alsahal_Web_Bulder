import BuilderClient from '@/components/builder/BuilderClient'

export const metadata = {
  title: 'Builder — Alsahal WebBuilder',
}

export default function BuilderPage() {
  return <BuilderClient />
}
